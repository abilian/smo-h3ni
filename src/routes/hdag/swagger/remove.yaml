summary: Delete graph
description: Remove a running graph
tags:
  - Graph
parameters:
  - name: name
    in: path
    description: Graph that will be deleted
    required: True
    type: string
responses:
  200:
    description: Successful graph deletion
  404:
    description: Graph with given name not found